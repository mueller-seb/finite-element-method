function [ value ] = gaussQuad( func, nodes, N )
%INT Gaussian quadrature switch for both triangles and squares
%   N is the order of Gaussia

%dim(P_N) = 1 for triangle shape functions (linear) and dim(P_N) = 2 for square
%shape functions (bilinear)
%N=3 is sufficient for quadratic polynomials (1+x+y+xy+x^2+y^2)

n = size(nodes, 2);

if (n == 3)
    value = fastTriGaussQuad(func, nodes, N);
elseif (n == 4)
    value = qdrGaussQuad(func, nodes, N);
end

end

